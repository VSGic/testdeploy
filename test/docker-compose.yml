version: '3'

services:
  tester:
    image: locustio/locust
    volumes:
      - ./:/mnt/locust
    command: >
      -f /mnt/locust/locustfile.py 
      --only-summary
      --headless
      -u 300
      -r 300 
      -t 180
      --html app_loadreport
      --host http://app:5000
